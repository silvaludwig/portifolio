services:
  - type: web
    name: portfolio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn meu_site.meu_site.wsgi:application --chdir meu_site
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: meu_site.meu_site.settings
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
